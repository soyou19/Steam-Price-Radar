# 安全政策

## 报告漏洞

如果你发现了安全问题，**请不要开公开 issue**。

请通过 GitHub 的 [私密漏洞报告](../../security/advisories/new) 功能提交，
或在仓库的 Security 标签页选择 "Report a vulnerability"。

请在报告中包含：

- 受影响的版本/提交
- 复现步骤（最小可复现即可）
- 影响评估（能否读取本地文件、能否 SSRF、能否执行代码等）

我会尽快确认并回复。修复发布后会在 CHANGELOG 里致谢（除非你希望匿名）。

---

## 项目的安全模型

### 部署假设

本服务设计为**本机/内网自用工具**，默认监听 `127.0.0.1`，**没有身份认证**。
若要暴露到公网，请自行加反向代理与鉴权，并注意：

- `/api/refresh` 是 **POST 且无鉴权**，任何人都能触发一次抓取（消耗抓取配额）
- 页面与 API 会展示抓取到的第三方内容（Steam / GamerPower）

### 已做的防护

| 面向的风险 | 处理方式 |
| --- | --- |
| 目录穿越读取工作区文件 | 静态资源解析后强制校验仍在对应根目录内（`public/` 与 `bench/` 各自独立校验），并有测试覆盖 |
| 外部数据的 XSS | 前端所有插值走 `escapeHtml`；URL 只允许 http/https（`safeUrl`） |
| 外部数据注入的恶意 URL | GamerPower 等外部字段的 URL 会校验协议，拒绝 `javascript:` / `data:` |
| 原型污染 | 解析外部 JSON 后只读取白名单字段，不做 `Object.assign` 到原型 |
| 无界内存增长 | 事件列表有上限；候选队列、条目表都有明确的数据治理策略 |

### 已知的、有意为之的行为

- 服务会**主动访问外部网络**（Steam、GamerPower，以及你配置的加速器代理）。
  使用第三方加速器时，HTTPS 流量会经过它的 MITM 代理 —— 这是加速器的工作方式，
  是否信任由你决定。项目提供 `PROXY_URL=none` 强制直连。
- 使用加速器时需要 `--use-system-ca` 来信任其自签证书。这会**信任系统证书库中的
  全部根证书**，请确认你接受这一点。（该选项需要 Node >= 22.15 / v23.8；
  完整的代理支持需要 Node >= 22.21.0，见 [环境依赖](docs/requirements.md)。）
- 抓取结果会完整落盘到 `DATA_DIR/state.json`（默认 `.data/`），不加密。

### 支持的版本

只维护 `main` 分支上的最新提交。安全修复不回溯到旧提交，
因为本项目是本地自用工具、没有服务端部署形态。

## 依赖

运行时**零第三方依赖**，只用 Node 内置模块。这显著缩小了供应链攻击面，
也是本项目刻意维持的约束（见 [CONTRIBUTING.md](CONTRIBUTING.md)）。

因此本项目的漏洞面主要来自三处：**自写的 HTTP/静态资源服务**、
**对不可信外部数据的解析**、**落盘的存档内容**。前两者有测试覆盖（见
[docs/architecture.md](docs/architecture.md) 与 `test/api.test.js`）。
